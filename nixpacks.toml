# Tells Railway's Nixpacks builder which Node version and how to build/start.
# Vite builds to /dist; we serve those static files with `serve`.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
